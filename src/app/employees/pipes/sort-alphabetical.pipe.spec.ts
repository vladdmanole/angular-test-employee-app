import { SortAlphabeticalPipe } from './sort-alphabetical.pipe';

describe('SortAlphabeticalPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAlphabeticalPipe();
    expect(pipe).toBeTruthy();
  });
});
